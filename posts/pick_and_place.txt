Upgraded from pushing to picking — same approach, harder task, it generalized. 🤖

Pick up an object, place it at the target location. Object at a different position every episode. 40 demos, trained both ACT and SmolVLA on an RTX 3060.

Two things changed from the last post:

A wrist camera — the gripper needs to know exactly when to close. A side-view phone camera doesn't give that. The wrist camera does.

A uniform yellow backdrop — so the policy isn't tied to one room. Same setup should work elsewhere without retraining.

𝗔𝗖𝗧 — smooth, stable, picks consistently from varied positions. ✅
𝗦𝗺𝗼𝗹𝗩𝗟𝗔 — works, but more jitter in the trajectory. ⚠️

Same finding as the first post: ACT more reliable on precision tasks.

𝗪𝗵𝗮𝘁'𝘀 𝗻𝗲𝘅𝘁
Language-conditioned multi-task — one policy, multiple tasks, controlled by natural language instructions. SmolVLA understands language, so no separate policies per task. Also working on reducing the SmolVLA jitter.

What would you try next? 👇
Relevant links in the first comment ↓
